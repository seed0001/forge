# Custom voices (XTTS)

Drop a short reference `.wav` clip of a voice here (a few seconds of clean
speech is enough) and it becomes selectable as a voice for the XTTS provider
in Settings → Voice output. Requires a local XTTS/Coqui server running —
see `TTS_XTTS_SERVER_URL` in `.env.example`.
